#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: Variable value
 * Return: Returns 1 or 2
 */
int _isupper(int c)
{
	if (c>='A' && c<= 'B')
	{
		return (1);
	}
			else
			{
				return (0);
			}
}
